# Copy this file to .env locally. Never commit real secrets.
NODE_ENV=development
PORT=3000

# Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs -> Web application
GOOGLE_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# Generate with: openssl rand -hex 32
SESSION_SECRET=replace-with-a-long-random-secret

# Comma-separated allowed origins. Add production site URL when deployed.
ALLOWED_ORIGINS=http://localhost:3000
